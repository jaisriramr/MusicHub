import { Fragment } from "react";
import "./playlist.css";
import { useParams } from "react-router-dom";

const Playlist = () => {
  return (
    <Fragment>
      <div className="playlist-container">Playlist container</div>
    </Fragment>
  );
};

export default Playlist;
